name: build

on:
  pull_request:
    branches: [dev, main]


jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v2
      - name: Build Container
        run: docker build -t tromega-frontend .